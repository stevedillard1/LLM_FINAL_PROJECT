# =============================================================================
# config.yaml — Experiment configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Model configuration — uncomment the preset you want to use
# -----------------------------------------------------------------------------

# Gemma-2-4b — best results, needs A10 GPU (24GB VRAM)
# model_id:    "google/gemma-2-9b-it"
# sae_release: "gemma-scope-9b-it-res"
# sae_hook:    "blocks.31.hook_resid_post"
# sae_id:      "layer_31/width_16k/average_l0_43"

# Gemma-3-1b — newer architecture, fits on most GPUs (8GB+)
# model_id:    "google/gemma-3-1b-it"
# sae_release: "gemma-3-1b-res-matryoshka-dc"
# sae_hook:    "blocks.13.hook_resid_post"
# sae_id:      "blocks.13.hook_resid_post"

# Pythia-70m — CPU-safe, fast, low accuracy — good for smoke testing
model_id:    "EleutherAI/pythia-70m-deduped"
sae_release: "pythia-70m-deduped-res-sm"
sae_hook:    "blocks.3.hook_resid_post"
sae_id:      "blocks.3.hook_resid_post"

# -----------------------------------------------------------------------------
# Experiment parameters
# -----------------------------------------------------------------------------
n_profile:  400
n_eval:     417
top_k:      100
n_wikitext: 100
output:     "results.json"